<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/styles.css">
    <title>Inventory App</title>
</head>
<body>
    <%- include('partials/header') %>
    <div class="movie-details-container">
        <div class="movie-details">
            <img src="<%= movie.image_url%>" alt="movie poster"/>
            <div>
                <div>
                    <h2 class="movie-title"><%= movie.title%></h2>
                    <div class="movie-text">
                        <div><%= movie.description%></div>
                        <div>Director: <%= movie.director%></div>
                        <div>Released: <%= new Date(movie.release_date).toLocaleDateString('en-US')%></div>
                    </div>
                    <h2 class="movie-price">$<%= movie.price%></h2>
                </div>
                <div class="movie-buttons">  
                    <% if (!locals.confirm) {%>
                        <form method="GET" action="/movies/<%= movie.id%>/delete">
                            <button>Delete</button>
                        </form>
                        <form method="GET" action="/movies/<%= movie.id%>/edit">
                            <button>Edit</button>
                        </form> 
                    <% } else { %>
                        <form class="confirm-form" method="POST" action="/movies/<%= movie.id%>/delete">
                            <input name="confirmPassword" id="confirmPassword" type="password" maxlength="255" placeholder="Enter admin password" required/>
                            <button>Confirm Delete</button>
                        </form>
                    <% } %>        
                </div>
                <%- include('partials/errors.ejs') %>
            </div>
        </div>
    </div>
</body>
</html>